import React from 'react'

import styles from './itemCart.module.css'

export default function ItemCart({ cardTitle, cardText, image, loadProduct, price, weight }) {
  return (
    <div className={styles.card}>
    </div>
  )
}
